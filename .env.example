# Sellasist Connector - Environment variables
PORT=8000
LOG_LEVEL=DEBUG
STORAGE_DIR=/tmp/sellasist_installations

# Encryption key for shared_secret at rest
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# REQUIRED for production - without it shared_secret is stored as plaintext
ENCRYPTION_KEY=
